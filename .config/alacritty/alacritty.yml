# Configuration for Alacritty, the GPU enhanced terminal emulator.

# Setup term for programs to be able to use it
env:
  TERM: xterm-256color

window:
  decorations: None
  # Spread additional padding evenly around the terminal content.
  dynamic_padding: true

  # Startup Mode (changes require restart)
  # startup_mode: Maximized

  opacity: 0.95

# shell
shell:
  program: /bin/zsh
  args:
    - --login

font:
  # Normal (roman) font face
  normal:
    family: MesloLGS NF
    # The `style` can be specified to pick a specific face.
    style: Regular

  # Bold font face
  bold:
    family: MesloLGS NF
    # The `style` can be specified to pick a specific face.
    style: Bold

  # Italic font face
  italic:
    family: MesloLGS NF
    # The `style` can be specified to pick a specific face.
    style: Italic
 
  # Bold Italic Font face
  bold_italic:
    family: MesloLGS NF
    style: Bold Italic

  # Point size of the font
  size: 14.0

# Offset is the extra space around each character. `offset.y` can be thought of
# as modifying the line spacing, and `offset.x` as modifying the letter spacing
# I've given in 14 spacing which fits really well with my fonts, you may change it
# to your convenience but make sure to adjust 'glyph_offset' appropriately post that
offset:
  x: 0
  y: 14

# Note: This requires RESTART
# By default when you change the offset above you'll see an issue, where the texts are bottom
# aligned with the cursor, this is to make sure they center align.
# This offset should usually be 1/2 of the above offset-y being set.
glyph_offset:
  x: 0
  # Keeping this as half of offset to vertically align the text in cursor
  y: 7

# Better font rendering for mac
use_thin_strokes: true

selection:
  semantic_escape_chars: ",│`|:\"' ()[]{}<>\t"

  # When set to `true`, selected text will be copied to the primary clipboard.
  save_to_clipboard: true

# Live config reload (changes require restart)
live_config_reload: true

colors:
  primary:
    background: '#2e3440'
    foreground: '#d8dee9'
    dim_foreground: '#a5abb6'
  cursor:
    text: '#2e3440'
    cursor: '#d8dee9'
  vi_mode_cursor:
    text: '#2e3440'
    cursor: '#d8dee9'
  selection:
    text: CellForeground
    background: '#4c566a'
  search:
    matches:
      foreground: CellBackground
      background: '#88c0d0'
    bar:
      background: '#434c5e'
      foreground: '#d8dee9'
  normal:
    black: '#3b4252'
    red: '#bf616a'
    green: '#a3be8c'
    yellow: '#ebcb8b'
    blue: '#81a1c1'
    magenta: '#b48ead'
    cyan: '#88c0d0'
    white: '#e5e9f0'
  bright:
    black: '#4c566a'
    red: '#bf616a'
    green: '#a3be8c'
    yellow: '#ebcb8b'
    blue: '#81a1c1'
    magenta: '#b48ead'
    cyan: '#8fbcbb'
    white: '#eceff4'
  dim:
    black: '#373e4d'
    red: '#94545d'
    green: '#809575'
    yellow: '#b29e75'
    blue: '#68809a'
    magenta: '#8c738c'
    cyan: '#6d96a5'
    white: '#aeb3bb'

cursor:
  # Cursor style
  #
  # Values for `style`:
  #   - ▇ Block
  #   - _ Underline
  #   - | Beam
  style: Block

  # If this is `true`, the cursor will be rendered as a hollow box when the
  # window is not focused.
  unfocused_hollow: true

bell:
  animation: EaseOutExpo
  duration: 75

# Keybinds
# key_bindings:
  # - { key: Return, mods: Shift, chars: "\x1e"}
  # - { key: Return, mods: Control, chars: "\x1f"}
  # - { key: K, mods: Command, chars: "\x63\x6f\x6e\x64\x61\x20\x64\x65\x61\x63\x74\x69\x76\x61\x74\x65\x0a\x63\x6f\x6e\x64\x61\x20\x61\x63\x74\x69\x76\x61\x74\x65\x20\x62\x61\x73\x65\x0a"}
  # - { key: C, mods: Alt, chars: "\x1bc"}
  # - { key: T, mods: Alt, chars: "\x1bt"}
  # - { key: S, mods: Alt, chars: "\x1bs"}
  # - { key: A, mods: Alt, chars: "\x1ba"}
  # - { key: 0, mods: Alt, chars: "\x1b0"}
  # - { key: J, mods: Alt, chars: "\x1bj"}
  # - { key: I, mods: Alt, chars: "\x1bi"}
  # - { key: Space, mods: Control, chars: "\x00" }
